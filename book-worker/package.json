{
  "name": "book-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to ../frontend/.wrangler/state",
    "start": "wrangler dev --persist-to ../frontend/.wrangler/state",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/dompurify": "^3.0.5",
    "@types/mozilla-readability": "^0.2.1",
    "@types/node": "^25.5.2",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.81.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dompurify": "^3.3.3",
    "fast-xml-parser": "^5.5.10",
    "fflate": "^0.8.2",
    "linkedom": "^0.18.12"
  }
}
